"""
Models package — 3 gesture recognition algorithms (Ma'am's Step 3)

  cnn_model.py      : GestureCNN (Convolutional Neural Network)
                      Also available via: from code.train_cnn import GestureCNN

  lstm_model.py     : LSTMModel (Recurrent Neural Network / LSTM)
                      Also available via: from code.train_lstm import LSTMModel

  svm_classifier.py : SVMClassifier (Support Vector Machine)
                      Also available via: from code.train_svm import build_svm_pipeline

All three models are trained separately — see:
  python code/train_cnn.py  --help
  python code/train_lstm.py --help
  python code/train_svm.py  --help
"""
